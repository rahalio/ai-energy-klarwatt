/**
 * ProposePaymentPlan handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: resolutions
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { proposePaymentPlan } from "@klarwatt/api-server/domains/resolutions/handlers";
import type { ResolutionsDDDDependencies } from "@klarwatt/api-server/domains/resolutions";
import { createPaymentPlan as createPaymentPlanEntity } from "@klarwatt/factories/resolutions";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@klarwatt/tests/shared";
import { RESOLUTIONS_USE_CASES } from "../__factories__/use-cases.js";

describe("ProposePaymentPlan", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: ResolutionsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<ResolutionsDDDDependencies>(
      RESOLUTIONS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 201 with created entity", async () => {
    const input = {} as any;
    const entity = createPaymentPlanEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.body = input;
    vi.spyOn(mockDeps.executeProposePaymentPlan, "execute").mockResolvedValue(useCaseResult);

    await proposePaymentPlan(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeProposePaymentPlan.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(201);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
