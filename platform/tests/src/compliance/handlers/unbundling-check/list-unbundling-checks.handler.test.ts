/**
 * ListUnbundlingChecks handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: compliance
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { listUnbundlingChecks } from "@klarwatt/api-server/domains/compliance/handlers";
import type { ComplianceDDDDependencies } from "@klarwatt/api-server/domains/compliance";
import { createUnbundlingCheck as createUnbundlingCheckEntity } from "@klarwatt/factories/compliance";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@klarwatt/tests/shared";
import { COMPLIANCE_USE_CASES } from "../__factories__/use-cases.js";

describe("ListUnbundlingChecks", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: ComplianceDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<ComplianceDDDDependencies>(
      COMPLIANCE_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with list of entities", async () => {
    const entities = [
      createUnbundlingCheckEntity({ orgId }),
      createUnbundlingCheckEntity({ orgId }),
    ];
    const useCaseResult = {
      data: { items: entities, nextCursor: undefined, prevCursor: undefined },
      meta: { correlationId: "test", timestamp: new Date().toISOString(), pagination: {} }
    };
    
    vi.spyOn(mockDeps.executeListUnbundlingChecks, "execute").mockResolvedValue(useCaseResult);

    await listUnbundlingChecks(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeListUnbundlingChecks.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
