{
  "name": "@klarwatt/tests",
  "version": "0.1.0",
  "private": true,
  "description": "Vitest shared helpers for Klarwatt platform tests",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@klarwatt/core": "workspace:*",
    "@klarwatt/services": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.9"
  }
}
