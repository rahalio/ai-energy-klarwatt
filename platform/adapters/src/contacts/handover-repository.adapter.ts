/**
 * HandoverRepositoryAdapter - Port Adapter Implementation
 *
 * Generated by Port Adapter Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: contacts
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * HandoverRepositoryAdapter
 *
 * DDD: Infrastructure adapter that implements HandoverRepository port.
 * Handles infrastructure concerns (HTTP, database, external APIs).
 * Encapsulates Ddb creation - api-server passes dynamoClient only.
 */

import type {
  HandoverRepository,
} from "@klarwatt/services/contacts";
import type { AdapterDynamoDBClient } from "../_shared/dynamodb-client-types.js";
import { HandoverRepositoryDdb } from "./handover-repository.ddb.js";

/**
 * HandoverRepositoryAdapter
 *
 * Implements HandoverRepository port for infrastructure layer.
 */
export class HandoverRepositoryAdapter implements HandoverRepository {
  private readonly ddb: HandoverRepositoryDdb;

  constructor(
    private readonly dynamoClient: AdapterDynamoDBClient
  ) {
    this.ddb = new HandoverRepositoryDdb(this.dynamoClient);
  }

  async escalateToAdvisor(input: Parameters<HandoverRepository['escalateToAdvisor']>[0]): Promise<Awaited<ReturnType<HandoverRepository['escalateToAdvisor']>>> {
    // Delegate to DynamoDB repository
    return await this.ddb.escalateToAdvisor(input);
  }
}
