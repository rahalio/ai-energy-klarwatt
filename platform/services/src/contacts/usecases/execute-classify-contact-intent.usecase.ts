/**
 * ExecuteClassifyContactIntent - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: contacts
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteClassifyContactIntent Use Case
 *
 * DDD: Application use case for get Intent.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { ClassifyContactIntentInput, ClassifyContactIntentOutput } from "../dto/intent.dto";
import type { ExecutionContextService, IdGeneratorService } from "@klarwatt/services/_shared/index.js";
import type { IntentRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteClassifyContactIntent {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly intent: IntentRepository
  ) {}

  async execute(input: ClassifyContactIntentInput): Promise<ClassifyContactIntentOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.ctcId();

    // All field names extracted from OpenAPI spec: utterance
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).sessionId) {
      throw new ValidationError("sessionId is required");
    }
    const sessionId = (input as any).sessionId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.intent.classifyContactIntent({ sessionId: sessionId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${sessionId}`);
    }

    return entity as ClassifyContactIntentOutput;
  }
}
