/**
 * ExecuteEscalateToAdvisor - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: contacts
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteEscalateToAdvisor Use Case
 *
 * DDD: Application use case for get Handover.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { EscalateToAdvisorInput, EscalateToAdvisorOutput } from "../dto/handover.dto";
import type { ExecutionContextService, IdGeneratorService } from "@klarwatt/services/_shared/index.js";
import type { HandoverRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteEscalateToAdvisor {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly handover: HandoverRepository
  ) {}

  async execute(input: EscalateToAdvisorInput): Promise<EscalateToAdvisorOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.ctcId();

    // All field names extracted from OpenAPI spec: reason, targetQueue
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).sessionId) {
      throw new ValidationError("sessionId is required");
    }
    const sessionId = (input as any).sessionId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.handover.escalateToAdvisor({ sessionId: sessionId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${sessionId}`);
    }

    return entity as EscalateToAdvisorOutput;
  }
}
