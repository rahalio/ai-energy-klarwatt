/**
 * ExecuteGetContactSession - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: contacts
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetContactSession Use Case
 *
 * DDD: Application use case for get Contact.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetContactSessionInput, GetContactSessionOutput } from "../dto/contact.dto";
import type { ExecutionContextService, IdGeneratorService } from "@klarwatt/services/_shared/index.js";
import type { ContactRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteGetContactSession {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly contact: ContactRepository
  ) {}

  async execute(input: GetContactSessionInput): Promise<GetContactSessionOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.ctcId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).sessionId) {
      throw new ValidationError("sessionId is required");
    }
    const sessionId = (input as any).sessionId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.contact.getContactSession({ sessionId: sessionId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${sessionId}`);
    }

    return entity as GetContactSessionOutput;
  }
}
