/**
 * ExecuteRecordAdvisorCorrection - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: contacts
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteRecordAdvisorCorrection Use Case
 *
 * DDD: Application use case for get AdvisorCorrection.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { RecordAdvisorCorrectionInput, RecordAdvisorCorrectionOutput } from "../dto/advisor-correction.dto";
import type { ExecutionContextService, IdGeneratorService } from "@klarwatt/services/_shared/index.js";
import type { AdvisorCorrectionRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteRecordAdvisorCorrection {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly advisorCorrection: AdvisorCorrectionRepository
  ) {}

  async execute(input: RecordAdvisorCorrectionInput): Promise<RecordAdvisorCorrectionOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.ctcId();

    // All field names extracted from OpenAPI spec: advisorReference, correctExplanation, suspectedDataDefect, wasExplanationCorrect
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).sessionId) {
      throw new ValidationError("sessionId is required");
    }
    const sessionId = (input as any).sessionId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.advisorCorrection.recordAdvisorCorrection({ sessionId: sessionId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${sessionId}`);
    }

    return entity as RecordAdvisorCorrectionOutput;
  }
}
