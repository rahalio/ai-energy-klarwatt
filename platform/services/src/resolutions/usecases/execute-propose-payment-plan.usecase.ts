/**
 * ExecuteProposePaymentPlan - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: resolutions
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteProposePaymentPlan Use Case
 *
 * DDD: Application use case for get PaymentPlan.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { ProposePaymentPlanInput, ProposePaymentPlanOutput } from "../dto/payment-plan.dto";
import type { ExecutionContextService, IdGeneratorService } from "@klarwatt/services/_shared/index.js";
import type { PaymentPlanRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteProposePaymentPlan {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly paymentPlan: PaymentPlanRepository
  ) {}

  async execute(input: ProposePaymentPlanInput): Promise<ProposePaymentPlanOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.resId();

    // All field names extracted from OpenAPI spec: accountId, advisorReference, affordabilityPolicyRef, instalmentAmount, instalmentCount
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).accountId) {
      throw new ValidationError("accountId is required");
    }
    const accountId = (input as any).accountId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.paymentPlan.proposePaymentPlan({ accountId: accountId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${accountId}`);
    }

    return entity as ProposePaymentPlanOutput;
  }
}
