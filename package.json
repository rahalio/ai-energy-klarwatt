{
  "name": "klarwatt",
  "version": "0.1.0",
  "description": "Klarwatt — OpenAPI-first DDD monorepo \u2014 shared scaffolds + identity blueprint",
  "private": true,
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "lint": "eslint .",
    "lint:openapi": "pnpm --filter @klarwatt/openapi-core lint:domains",
    "bundle:openapi": "pnpm --filter @klarwatt/openapi-core bundle:domains",
    "build": "pnpm --filter @klarwatt/core build && pnpm --filter @klarwatt/services build && pnpm --filter @klarwatt/adapters build && pnpm --filter @klarwatt/api-server build",
    "dev:api": "pnpm --filter @klarwatt/api-server dev",
    "dev:web": "pnpm --filter @klarwatt/webapp dev",
    "build:web": "pnpm --filter @klarwatt/webapp build",
    "codegen:bootstrap": "node scripts/bootstrap-codegen.mjs",
    "codegen:core": "node scripts/sync-codegen-paths.mjs && PYTHONPATH=.codegen/codegen/src python3 -m zero_codegen.cli.main generate --all --layers core --config .codegen/.zero-codegen-merged.json --skip-build",
    "codegen:identity": "node scripts/sync-codegen-paths.mjs && PYTHONPATH=.codegen/codegen/src python3 -m zero_codegen.cli.main generate --domain identity --config .codegen/.zero-codegen-merged.json --skip-build",
    "codegen:paths": "node scripts/sync-codegen-paths.mjs"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "@types/node": "^20.0.0",
    "eslint": "^9.0.0",
    "prettier": "^3.8.1",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.55.0"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9.0.0"
  }
}
